# CLA signatures

Machine-managed branch. It stores the CLA signatures recorded by
`.github/workflows/cla.yml` under `signatures/v1/cla.json`.

Do not protect this branch: the CLA Assistant action must be able to commit to
it. Do not edit it by hand, and do not merge it into `main`.

The agreement itself lives in `CLA.md` on `main`.
